import { NameDecriptionFilterPipe } from './name-decription-filter.pipe';

describe('NameDecriptionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NameDecriptionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
